{
  "name": "ragify-chat",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "python3 -m venv .venv && . .venv/bin/activate && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt && streamlit run app.py"
  }
}